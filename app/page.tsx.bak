"use client";

import MappingStudio from "@/components/mapping-studio";
import ThemeToggle from "@/components/theme-toggle";

export default function Page() {
  return (
    <div className="min-h-screen">
      <header className="sticky top-0 z-50 border-b border-zinc-200 bg-white/80 backdrop-blur dark:border-zinc-800 dark:bg-zinc-950/70">
        <div className="mx-auto flex max-w-7xl items-center justify-between gap-3 px-4 py-3">
          <div className="flex items-baseline gap-3">
            <div className="text-lg font-semibold tracking-tight">MapForge</div>
            <div className="hidden text-sm text-zinc-500 dark:text-zinc-400 sm:block">
              Mapping Analysis Studio
            </div>
          </div>
          <div className="flex items-center gap-2">
            <ThemeToggle />
          </div>
        </div>
      </header>

      <main className="mx-auto max-w-7xl p-4">
        <MappingStudio />
      </main>

      <footer className="mx-auto max-w-7xl px-4 pb-8 pt-2 text-xs text-zinc-500 dark:text-zinc-400">
        Lokale App (State im Browser). Exportiert Excel ohne Formeln/Validierungen.
      </footer>
    </div>
  );
}
